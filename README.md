# pass-gen
chrome extension for generating readable sayable passwords
## setup chrome extension
go to chrome > three dots > extensions > manage extensions > enable developer mode > charger l'extension non empaquetée and select the unzipped normal folder `pass-gen`

## use
just click on the extension and the password will generate